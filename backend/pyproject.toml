[project]
name = "clinical-api"
version = "1.0.0"
description = "FastAPI backend for clinical management system"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
